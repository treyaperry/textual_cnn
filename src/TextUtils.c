#include "tcnn/TextUtils.h"

void TextUtils_to_grid_ids(const char* const text, const size_t width, const size_t maxRows) {
    // Placeholder implementation
    // Actual implementation to convert text to grid of character IDs goes here
    (void)text;
    (void)width;
    (void)maxRows;
}
